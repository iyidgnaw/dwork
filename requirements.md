# Personal Claude Cowork（基于 Opencode）需求文档

## 1. 项目目标
打造一个“我个人版本的 Claude Cowork”，基于 Opencode 现有能力，形成一个以“高效协作 + 可控上下文 + 可追踪交付”为核心的个人 AI 开发工作台。

## 2. 背景与问题陈述
当前使用通用 AI 客户端时，常见问题包括：
- 会话上下文与本地代码库脱节，跨任务记忆和引用成本高。
- 多模型/多 Provider 的切换流程不稳定，策略不透明。
- 任务拆解与执行记录分散在聊天、笔记和 issue 系统中，难以复盘。
- 自动化操作（执行命令、读写文件、生成 patch）缺乏统一的安全边界和审计轨迹。

## 3. 产品范围

### 3.1 In Scope（本期范围）
1. **个人工作区与项目上下文管理**
   - 支持管理多个本地项目工作区。
   - 提供会话与项目目录、分支、变更集之间的关联。
2. **Agent 协作式会话体验**
   - 支持“主会话 + 子任务”模式，便于并行推进。
   - 支持任务状态流转（todo / doing / blocked / done）。
3. **任务驱动开发（Spec → Design → Task）流程支持**
   - 在产品内直接引用 `requirements.md`、`design.md`、`task.md`。
   - 支持从 Task 条目触发执行上下文（命令、文件、测试）。
4. **本地执行与安全控制**
   - 提供命令执行、文件修改、测试运行能力。
   - 提供“可预览、可回滚、可审计”的操作链路。
5. **结果交付与复盘**
   - 自动汇总变更、测试结果、风险点与后续建议。

### 3.2 Out of Scope（本期不做）
- 团队级权限体系与复杂 RBAC。
- 云端多租户托管。
- 企业级计费、配额、组织结算。
- 移动端原生应用。

## 4. 用户画像与核心场景

### 4.1 目标用户
- 单人开发者（主要用户）。
- 兼职 PM/架构师角色的技术负责人（次要用户）。

### 4.2 核心场景
1. 新需求进入后，先在项目内完成 Spec 文档沉淀。
2. 基于 Task 列表将工作分配给多个 coding agent 并行执行。
3. 在每个 Task 中查看上下文、运行命令、生成改动并验证测试。
4. 汇总所有 Task 的结果生成最终交付说明。

## 5. 功能性需求

### FR-1 会话与项目绑定
- 系统必须支持将会话绑定到指定本地项目路径。
- 系统必须展示当前分支、未提交变更与最近提交信息。

### FR-2 上下文注入
- 系统必须支持将指定文件、目录和历史会话片段注入当前上下文。
- 系统应支持上下文来源标记（代码、命令输出、文档）。

### FR-3 Task 协作面板
- 系统必须支持创建/编辑/删除 Task。
- 每个 Task 必须包含字段：`id`、`title`、`description`、`assignee`、`status`、`dependencies`、`acceptance_criteria`。
- 系统应支持按 assignee、status 过滤。

### FR-4 执行能力
- 系统必须支持在受控终端中执行命令并回填结果。
- 系统必须支持基于 Task 上下文创建 patch。
- 系统应支持“一键运行推荐测试命令”。

### FR-5 安全与审计
- 系统必须记录关键操作日志（命令执行、文件变更、提交行为）。
- 系统必须支持在应用内查看单任务操作时间线。

### FR-6 交付总结
- 系统必须支持自动生成任务级与会话级总结。
- 总结必须包含：改动文件、测试结果、风险项、待办项。

## 6. 非功能性需求
- **性能**：常见交互（切换任务、打开会话）响应时间 < 1.5s（本地开发环境基线）。
- **稳定性**：长会话（>200 轮消息）不崩溃，且能保留可检索历史。
- **可维护性**：核心流程（会话、任务、执行器）需具备清晰模块边界。
- **可扩展性**：模型 Provider 与工具调用接口可插拔。
- **可观测性**：关键操作具备可查询日志与错误追踪标识。

## 7. 约束与依赖
- 基于 Opencode 现有架构与技术栈实现。
- 优先复用已有会话、终端、项目管理能力。
- 本期以本地优先（local-first）为原则，不依赖云端强一致服务。

## 8. 验收标准（高层）
1. 用户可在单项目内完成“需求 → 设计 → 任务 → 执行 → 总结”完整闭环。
2. 用户可通过 assignee 维度管理多 agent 任务分工。
3. 至少完成一个端到端示例：从 Task 创建到提交结果生成。
4. 核心流程具备可追踪日志，出现失败时可定位到具体步骤。
